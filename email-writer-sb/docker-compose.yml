services:
  email-writer-sb:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - GEMINI_URL=${GEMINI_URL}
      - GEMINI_APIKEY=${GEMINI_APIKEY}
    restart: unless-stopped
